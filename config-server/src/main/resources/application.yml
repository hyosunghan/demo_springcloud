server:
  port: 8769

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hyosunghan/springcloud
          search-paths: config
          username:
          password:
#        native:
#          search-locations: classpath:/shared
      label: master